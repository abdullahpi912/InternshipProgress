$kind: http-request
name: POST /api/predict - No request body
description: "Send POST with no body at all. Expected: 400 Bad Request, API must not crash."
url: "{{base_url}}/api/predict"
method: POST
headers:
  Content-Type: application/json
examples: ./.resources/POST -api-predict - No request body.resources/examples
order: 6000
